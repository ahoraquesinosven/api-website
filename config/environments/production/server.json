{
  "host": "${process.env.HOST}",
  "port": "${process.env.PORT}",
  "production": true,
  "proxy": {
    "enabled": true,
    "ssl": true,
    "host": "${process.env.HOST}",
    "port": "${process.env.PORT}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
